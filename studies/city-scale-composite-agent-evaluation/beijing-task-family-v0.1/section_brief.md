# 章节任务说明与修改记录

## 2026-09-04 最小输入准入续接

- Document：`minimal_task.md`；模式为 draft，不润色旧稿。
- Reader question：昨晚未闭合的任务如何落实到一份真实输入、必要输出与可检查的验收？
- Section job：将难度优先原则落实为一个可审阅的产品描述任务，保留恢复材料与正式输入的区别。
- Authority：研究者要求继续原目标；09-04 难度优先决定；canonical QA、配置和本轮只读恢复预检。
- Evidence route：`input_audit.json` 记录五个文件的路径、大小、当前 SHA-256、元数据、有限解码和来源指针；不是历史字节一致性或科学有效性证明。
- State：未冻结、未复制栅格、未运行模型实验，not-assessed；读取元数据／窗口是准入预检，不是新的科学结果。
- Protected：strict500 与其他方法不混用；有效像元组成不是面积占比；全图 QA 不能用作 BJuforebv4 内结果；参考计算仍待执行和科学准入。
- Contract/citations：`minimal_contract.json`；仅本地证据，不改 `references.bib`，不新增文献主张。
- Paragraph map：未完目标回忆 → 实物发现 → 一个问题及输入 → 最小输出 → 参考与验收 → 一次研究者确认。
- Non-goals：新制图、重训练、下载、复制恢复数据、方法冻结、科学 gold、模型比较和更改 Ch2 顺序。

| ID | 类型 | 此前 | 本轮变化 | 依据 |
| --- | --- | --- | --- | --- |
| A10 | argument | 只核查 canonical 路径，输入未到位 | 五份实物在已知恢复路径找到，仍为 recovery candidate；记录当前 hash 和元数据 | 15 个精确路径检查及只读预检 |
| A11 | argument | 首轮描述任务尚未绑定产品 | 建议 strict500 多类图＋既定研究区，先不另造分区；二值图仅校验派生关系 | 当前元数据、QA 与难度优先决定 |
| A12 | argument | 比例／面积与旧 QA 可能被混用 | 锁定产品有效像元组成口径；空间范围不一致的旧比例不能当答案 | 栅格 EPSG:4326、AOI bbox、全图 QA 源码 |
| A13 | verification | 合同未锁关键标识，窗口仅记比例位置 | 补 method／类别／nodata／AOI／CRS／历史身份限制；记录精确窗口并复放 15 窗 | 独立 V1 两项 P2，不改变科学方案 |
| L3 | language | 无独立润色需求 | 不改旧正文语言与旧规格批准字段 | 历史保留原则 |

## 2026-09-04 难度修订说明

- Writing mode：argument edit；本轮不做旧正文语言润色。
- Reader question：首轮哪些指标较容易可靠完成，如何保留真实城市问题而不引入不必要的方法负担？
- Section job：`difficulty_review.md` 给出全目录的分组难度、实际就绪性和较低成本候选；不是正式指标标准。
- Authority：研究者本轮直接修订，记录于 `DEC-2026-0904-beijing-difficulty-first-candidate.md`；Ch3 指标矩阵、配置、历史运行记录与本机指定路径检查。
- Approved direction：难度优先、指标体系不承担新颖性、允许暂缓高阶结构和生态功能；具体指标未冻结。
- Evidence route：每一分组附来源编号；存在性表只支持指定路径状态，历史运行不等于本机可复算。
- Required boundary：低计算成本不等于科学有效；植被类别比例不等于冠层覆盖度；不以像元计数支持论文最终面积；不构造虚假的独立任务数。
- Terms/state：核心栖息地、候选、not-assessed、未运行实验；完整 RSS 与 quick corridor baseline 保持区分。
- Contract/citations：`difficulty_contract.json`；仅引用本地原始记录，不新增论文引用，不改 `references.bib` 或文献证据等级。
- Paragraph map：修订结论 → 旧分类定位 → 四轴分级 → 实物状态 → 最小问题与输出 → 升级条件与验收 → 核验记录。
- Non-goals：正式章节范围、Ch2 顺序、Route B 方法、评分阈值、实验或数据下载；不把旧规格改成批准状态。

| ID | 类型 | 原状态 | 修订 | 依据 |
| --- | --- | --- | --- | --- |
| A6 | argument | 配置衔接被用作空间格局／连通性首轮优先理由 | 按数据、计算、科学验收、复现分别评估，不再必做形态／连通性 | 09-04 研究者修订及实际路径检查 |
| A7 | argument | 首轮先固定三个分析子问题 | 先准备一个完整描述性问题与必要输出，不把输出列数当任务数 | 控制复杂度且保留真实性 |
| A8 | argument | 目录被默认为待实现范围 | 全目录分组评估，较低成本候选先行，高阶条件不具备则暂缓 | 指标体系可调整，不承担新颖性主张 |
| A9 | argument | 单年产品被默认可支持所有组成；裸地类别与生态裸土暴露的差别不够显式 | 按多类／二值输入限制输出，不为组成新制图；裸地类别只是受限代理，原生态量仍未覆盖 | 独立 V1 两项 P2 与 Level 1 QA |
| L2 | language | 旧稿可读 | 不进行语言润色；保留 09-03 历史内容，仅加状态指针 | 避免混同论证修改与语言编辑 |

## 2026-09-03 原始候选说明（历史）

- 模式：候选论证／研究规格准备，不修改开题正文。
- 读者问题：北京案例如何形成相互依赖的任务群，并把第二章知识支持与第三章任务验收接起来？
- 本节职责：给研究者一个有限范围选择，而不是要求审核全部指标或全部知识条目。
- 权威：现行 THESIS_STATE、09-02 城市森林问题牵引与 Route B-first 决定；最新 Ch2 两任务先行授权由该任务所属线程维护。
- 允许的事实：指标矩阵、配置、历史制品记录及本机指定路径的存在性；不据此推断地图或生态解释有效。
- 文献来源：PaperQA2 第 2 节、ClimateAgent 第 4.1 节、EarthVerse 第 3.3 节与附录 A.3.1；只读相关全文段落，非整篇全面复核。
- 证据状态：来源内容、历史诊断、候选研究设计分开；BTF-C5 保持 hypothesis。
- 受保护边界：manual 301 为校准／训练；network_only 不等于 resistance_supported；Level 1 前景不是新批准的城市森林生态定义；目录不是就绪任务。
- 人工门：总体范围与对象定义；任务科学验收另行确认。未冻结、未运行实验、scientific validity 为 not-assessed。
- 非目标：改变正式标题、章级角色、优先级、Ch2 active brief、gold、评分阈值或实验配置；不写 Zotero，不恢复重型资产，不提交其他任务内容。
- 段落职责：README 为一个推荐及边界；design.md 为资产、任务、知识、验收和相邻工作的对照；verification.md 为机械检查与独立评审。

## 修改记录

| ID | 类型 | 此前状态 | 本轮准备 | 授权／依据 |
| --- | --- | --- | --- | --- |
| A1 | argument | 讨论已形成，但未绑定具体资产与任务依赖 | 一项总体问题、三个分析子问题和一个综合出口的候选规格 | 研究者要求按优先级推进至确认前 |
| A2 | argument | 结构与生态功能可能被同时承诺 | 第一轮建议聚焦空间格局和潜在连通性，详细结构与生态功能暂不纳入 | 注册项 draft/planned、重型产品本机缺失、验证边界 |
| A3 | argument | 知识增强可能仅体现规则防错 | 每一子任务同时列建设性知识作用与完成结果 | 本轮对话认可的问题表述 |
| A4 | argument | 真实环境、多步骤可能被当成新意 | 与相邻全文定位比较，差异命题保留为候选 | ledger.json，BTF-C2--C5 |
| A5 | argument | 初稿总问题必问敏感性，但具体任务没有必交敏感性结果 | 按独立 V1 意见收窄总问题，敏感性仅作有合法配对并另获准的扩展 | beijing_scope_v1，2026-09-03，P2 范围一致性发现 |
| L1 | language | 无独立待润色正文 | 未进行语言编辑或覆盖旧稿 | 不适用 before/after compare |
