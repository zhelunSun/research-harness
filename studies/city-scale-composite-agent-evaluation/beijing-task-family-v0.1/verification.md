# Verification record

> Current stage update, 2026-09-04: the researcher admitted the narrower product-description case and its reference calculation is complete. See [the actual result and closeout record](../../../../urbfo-agent-demo/experiments/results/beijing-product-description-v0.1/README.md). The later explicit commit/sync authorization supersedes local-only notes below for this thread's selected artifacts; raw input remains external. Earlier scope questions and asset availability are historical snapshots, not current blockers. The old proposal's live asset audit now flags only A05 (`PLAN.md`) hash drift: its recorded hash matches the file at `6f1482c`, while the current plan includes the admitted reference and closeout. No old hash is silently replaced. Nine mutation tests and proposal integrity with live-asset verification disabled pass; current input checks belong to the reference packet. User utility, scientific gold and matched system evaluation remain open.

> 2026-09-04: the original scope choice below is historical, not an unanswered current question. See `difficulty_review.md` and the dated addendum at the end for the researcher-directed revision. The original specification and its approval fields remain unchanged.

Status: V0 passed; independent V1 complete. The initial 0 P1 / 1 P2 finding was repaired and the reviewer confirmed P2 closed with no residual P1/P2 in the narrow recheck. V2 scope review remains pending.

This is a candidate research-specification audit, not a scientific grader, model experiment or geospatial rerun.
The existing thesis contracts, scientific gold, chapter-local task fixtures and accepted method states remain unchanged.

Files used: writing_contract.json, section_brief.md, specification.json, ledger.json and references.bib.
Located full-text passages were inspected for the three sources; source/claim admission remains needs_review,
and BTF-C5 remains hypothesis. No existing literature packet was upgraded or merged into the opening contract.

## Results

- Proposal audit: 4 nodes (3 analysis subquestions plus 1 synthesis), 23 explicit asset paths,
  shared base_map and object_definition, 0 integrity errors.
- 17 paths exist and retain the recorded SHA-256; 6 selected heavy-product paths are absent.
  Missing paths are retained as readiness blockers, not converted into failed scientific tasks or deleted rows.
- Unit checks: 9/9 passed, including dependency cycles, unlinked or absent outputs, unauthorized gold/scope,
  evaluation leakage, path escape and missing asset references.
- Literature ledger: 3 sources, 5 claims, 6 links; 0 errors and 0 warnings.
- README writing audit and design writing audit: both 0 errors and 0 warnings.
- The generated writing_bridge.json is merged into the packet-local design_contract.json.
  The short scope page uses writing_contract.json; neither is the formal opening-report writing contract.
- Workspace navigation: 0 issues. Human-gate audit at this checkpoint: 0 issues; its live counts may change as the
  concurrent Chapter 2 task updates the shared registry.
- Git whitespace check: no errors; existing CRLF/LF notices on other tracked files are not research findings.

These checks establish proposal consistency, source identity and explicit access boundaries only.
They do not establish taxonomy validity, numerical scientific correctness, novelty, ecological function or model efficacy.
No geospatial run, solver episode, expert study, remote research-content write or new scientific result was produced.

## Independent V1 and response

Reviewer: independent fresh-context read-only subagent beijing_scope_v1.

Original finding P2: the initial total question required sensitivity to map definitions and scale, while the
minimum outputs allowed sensitivity to remain unassessed. This would make the proposed acceptance internally incomplete.

Writer response: narrowed the total question and recommendation to spatial patterns, potential connectivity,
spatial differences and candidate weak links; changed S accordingly. Sensitivity is now explicitly optional,
requires a scientifically comparable pair and separate admission. No new experiment or parameter promise was added.
Argument change A5 is recorded in section_brief.md.

The reviewer found no additional P1/P2 on product-vs-configuration state, constructive knowledge benefits,
same-city dependence, representation confounding, leakage, alternate valid paths or human approval boundaries.
The reviewer did not independently reread the papers; literature entailment is not upgraded by this V1.

Narrow recheck: the reviewer reread README.md, design.md, specification.json, section_brief.md and both
writing contracts. It confirmed the scope correction, unchanged approval fields and retained V2 boundaries.

## Judgment audit

These scores are the writer's review aid, not an expert scientific rating.

| Dimension | Score / 2 | Basis |
| --- | --- | --- |
| Terminology | 2 | Canonical core-habitat terminology and three chapter roles retained |
| Numbers and states | 2 | Asset counts separated from task counts; scientific validity remains not-assessed |
| Claim/evidence fit | 1 | Located source descriptions and local facts are bounded; scientific admissibility and novelty remain open |
| Citation integrity | 2 | Existing keys preserved where available, new PaperQA2 intake identity explicit, checked locators and short excerpts |
| Paragraph function | 2 | Scope, assets, methods, comparison and gates have separate roles |
| Coherence | 2 | V1 scope mismatch repaired without adding an experiment commitment |
| Chinese readability | 2 | One-page scope decision precedes the technical appendix |
| Scope and limitations | 2 | No automatic promotion from scope acceptance or mechanical checks |

## Exact verification commands

Working directory: this packet directory.

```powershell
python audit_packet.py
python -m unittest test_audit_packet.py
python 'C:\Users\zhelunStation\.codex\skills\literature-evidence-ledger\scripts\audit_ledger.py' ledger.json --bib references.bib --bridge-out writing_bridge.json
python 'C:\Users\zhelunStation\.codex\skills\zh-scientific-writing\scripts\zh_scientific_audit.py' audit --text README.md --contract writing_contract.json --bib references.bib
python 'C:\Users\zhelunStation\.codex\skills\zh-scientific-writing\scripts\zh_scientific_audit.py' audit --text design.md --contract design_contract.json --bib references.bib
```

From the research-harness root:

```powershell
python scripts/audit_workspace_navigation.py
python scripts/audit_human_gates.py
git diff --check
```

After accepting a scope, copy or version the candidate before changing approval fields; this audit intentionally
rejects a silently promoted version. Scientific graders and experiment tools belong in the owning chapter, not here.

## Open decisions and synchronization

1. Current researcher question: accept the spatial-pattern/potential-connectivity minimum, or require detailed
   structure/ecological function now. No source is silently removed to make one choice appear feasible.
2. Later combined domain gate: object mapping, time/space units, necessary results, reference evidence and scientific acceptance.
3. Existing Ch2 specific-case admission and Ch3 matched-adapter gate remain independent. Already authorized Ch2
   ordering and reasonable spend are not reopened.
4. External product location, materialization, license and validation remain unknown beyond the tested paths.

Only this packet and its parent study README are written by this task. The concurrent Ch2 task owns the shared
execution plan, human gates and lock registry; handoff goes through that owner to prevent overlapping writers.
Pre-existing literature and Chapter 2 changes are preserved. Research content remains local and uncommitted;
no claim of cloud backup is made for this packet.

## 2026-09-04 difficulty-first addendum

Scope: user-directed argument revision, not language polishing or new science. The original README, design,
specification, results and prior V1 retain their historical meaning. New entry: `difficulty_review.md`.
Authority: `../../../decisions/DEC-2026-0904-beijing-difficulty-first-candidate.md`.
Contract: `difficulty_contract.json`; section objective and A6--A8 change ledger: `section_brief.md`.
No bibliography changes or fresh literature entailment claim were made.

V0 results:

- New review writing audit: 0 errors / 0 warnings. The first audit flagged only a long source-link list;
  the list was separated into readable source entries without changing meaning.
- 11 explicit candidate-input paths checked: 4 AOI components present, 7 products absent.
  All Markdown links and 4 contract authority paths exist. File presence is not geometry or scientific validation.
- Original candidate audit: 0 integrity errors; all 23 recorded source statuses/hashes still match.
  Original 9 mutation tests pass. These checks do not validate the new scientific task.
- Navigation: 0 issues. Human-gate audit after independent Ch2 authorization coordination: 12 total / 10 open /
  0 issues. The Beijing gate now concerns concrete input and scientific admission, not reapproval of the old scope.
- Start-of-turn fetched sync audit: all four branches ahead=0 / behind=0; expected dirty warnings in the control
  plane and Ch2, no chapter checkpoint mismatch. Research documents remain local/uncommitted and not cloud-backed.
- No Chapter 3 files, frozen mapping products, indicator configs or scientific references were changed.

Exact commands, from this packet directory:

```powershell
python C:/Users/zhelunStation/.codex/skills/zh-scientific-writing/scripts/zh_scientific_audit.py audit --text difficulty_review.md --contract difficulty_contract.json
python audit_packet.py
python -m unittest test_audit_packet.py
$reviewText = Get-Content -Raw difficulty_review.md
$pathRows = [regex]::Matches($reviewText, '(?m)^\| \x60(data/[^\x60]+)\x60 \| (存在|缺失) \|$')
if ($pathRows.Count -ne 11) { throw 'Expected 11 asset paths' }
foreach ($row in $pathRows) {
    $present = Test-Path -LiteralPath (Join-Path '../../../../urbfo-agent-demo' $row.Groups[1].Value) -PathType Leaf
    if ($present -ne ($row.Groups[2].Value -eq '存在')) { throw ('Path status drift: ' + $row.Groups[1].Value) }
}
foreach ($link in [regex]::Matches($reviewText, '\[[^\]]+\]\(([^)]+)\)')) {
    if (-not (Test-Path -LiteralPath $link.Groups[1].Value)) { throw ('Broken link: ' + $link.Groups[1].Value) }
}
$contractData = Get-Content -Raw difficulty_contract.json | ConvertFrom-Json
foreach ($authority in $contractData.document.authority) {
    if (-not (Test-Path -LiteralPath $authority)) { throw ('Missing authority: ' + $authority) }
}
```

Control-plane commands remain `python scripts/audit_human_gates.py`,
`python scripts/audit_workspace_navigation.py` and `git diff --check` from `research-harness/`.
No before/after number-preservation compare is claimed: this is an authorized argument-scope revision,
not a language-only edit. Existing bibliographic and unresolved markers remain in the historical packet.

V2 still open: concrete object/time/space definition, real task usefulness and scientific acceptance once an input
is ready. The difficulty-first direction itself has already been provided by the researcher; no immediate question
is required. Next action is a minimal input and acceptance list, without new mapping or connectivity computation.

Independent V1: fresh-context read-only reviewer `difficulty_v1` reported 0 P1 / 2 P2.
Both were repaired in a separate writer pass and confirmed closed in the reviewer's narrow recheck:

1. A binary product cannot deliver multiclass or bare-class composition. The question, difficulty row and
   required outputs now explicitly contract to foreground/other proportions when only a binary mask is available;
   no new mapping is introduced to fill the gap. The Level 1 QA is directly linked and was checked by both tracks.
2. A bare-land class fraction does not cover the original ecological quantity of bare-soil exposure within core
   habitats. The table interpretation now states that proxy limitation explicitly.

The original eight-dimension reviewer scores were terminology 1/2, number/state 2/2, claim/evidence 2/2,
local-source integrity 2/2, paragraph function 2/2, coherence 1/2, readability 2/2 and scope 2/2.
These are editorial review aids, not scientific ratings. The final narrow review confirms no residual findings
for the two amended points; it does not issue a new scientific validity assessment.
Final writing audit remains 0 errors / 0 warnings; all local links including the added Level 1 QA pass.

## 2026-09-04 resumed goal: minimal input and task admission

The previous range packet was delivered; the scientific task itself was not run. The resumed goal is bounded to
an actual input locator and one task/acceptance candidate, not a completed benchmark or a new Ch2 experiment.
Current artifact: `minimal_task.md`; writing contract: `minimal_contract.json`; evidence snapshot: `input_audit.json`.
A10--A12 in `section_brief.md` record argument changes. No bibliography or existing scientific-state change occurred.

New evidence:

- Five exact raster paths were checked under three known roots (15 checks, not a recursive recovery scan).
  All five exist in `D:/Projects/phd-research/urbfo-mapping`, none at their canonical or old `D:/codes/urban-forest-agent`
  counterparts. That recovery root remains read-only; no copy, move or source modification occurred.
- Fresh SHA-256 fingerprints and canonical source-record hashes were captured for every file. The four canonical
  AOI components were also hashed. These hashes pin current files only: no historical raster byte hash was located.
- All five GeoTIFFs opened; each decoded three 16x16 windows. Full-raster decoding and scientific validity were
  not assessed. Strict500 full-map profile matches the recorded QA; full/binary grids align. Pixelwise derivation
  consistency was not tested.
- AOI header/CRS reads show one shape record and a bounding box within the raster extent. Neither valid geometry
  nor actual valid-observation overlap follows from that check; both remain in admission preflight.
- Canonical QA code was read, not executed: it counts the full raster without applying the chosen AOI, and emits
  nominal areas. Its old counts/proportions are not reference results for the new AOI-specific task.

V0: minimal-task writing audit 0 errors / 0 warnings after separating long source-link sentences; all five raster
fingerprints, source pins, four AOI hashes and candidate links/authorities verified. Original packet audit and nine
mutation tests still pass. This is proposal/input identity verification, not a scientific reference computation.
No model episode, new map, geometry repair, reference count or independent validation was produced.

Exact repeated checks from the packet directory:

```powershell
python C:/Users/zhelunStation/.codex/skills/zh-scientific-writing/scripts/zh_scientific_audit.py audit --text minimal_task.md --contract minimal_contract.json
python audit_packet.py
python -m unittest test_audit_packet.py
$inputAudit = Get-Content -Raw input_audit.json | ConvertFrom-Json
foreach ($entry in $inputAudit.candidates) {
    if ((Get-FileHash -LiteralPath $entry.recovery_path -Algorithm SHA256).Hash.ToLower() -ne $entry.sha256) { throw ('Hash mismatch: ' + $entry.id) }
    $sourceFile = Join-Path '../../../../urbfo-agent-demo' $entry.source_locator
    if ((Get-FileHash -LiteralPath $sourceFile -Algorithm SHA256).Hash.ToLower() -ne $entry.source_locator_sha256) { throw ('Source hash mismatch: ' + $entry.id) }
}
foreach ($component in $inputAudit.aoi.components) {
    if ((Get-FileHash -LiteralPath $component.path -Algorithm SHA256).Hash.ToLower() -ne $component.sha256) { throw 'AOI hash drift' }
}
foreach ($fileName in @('minimal_task.md','difficulty_review.md')) {
    $page = Get-Content -Raw $fileName
    foreach ($link in [regex]::Matches($page, '\[[^\]]+\]\(([^)]+)\)')) {
        if (-not (Test-Path -LiteralPath $link.Groups[1].Value)) { throw ('Broken link: ' + $link.Groups[1].Value) }
    }
}
$minimalContract = Get-Content -Raw minimal_contract.json | ConvertFrom-Json
foreach ($source in $minimalContract.document.authority) {
    if (-not (Test-Path -LiteralPath $source)) { throw ('Missing authority: ' + $source) }
}
```

Pending V2 is now one concrete case/acceptance decision: strict500 2025 plus BJuforebv4 for product spatial
description and valid-pixel composition, followed by admitted-input preflight and reference preparation. It is not
approval of a new ecological definition, final area, scientific gold, model comparison or method-effect claim.
Current research artifacts remain local/uncommitted, separate from concurrently pushed Chapter 2 engineering.

Independent V1 (`minimal_input_v1`): 0 P1 / 2 P2. It found no promotion of recovery candidates, pixel-area
confusion, hidden extra task obligation or Ch2 priority change. The two P2 items were verification-contract gaps:
critical method/class/AOI/CRS/history literals were not protected, and window locations were not exact.
Writer repair added those literals (including class 0 versus nodata) and precise row/column offsets, dimensions,
band and truncation/read rules for every window. All 15 windows were reread and reproduced the stored value sets;
this does not imply whole-raster decoding or thematic validity. Final writing audit is still 0 errors / 0 warnings.

Original reviewer eight-dimension scores: terminology 2/2, number/state 1/2, claim/evidence 1/2, local-source
integrity 2/2, paragraph function 2/2, coherence 2/2, readability 2/2, scope 2/2. These are editorial aids only.

Final narrow V1 reread confirmed both P2 items closed with no new P1/P2. It checked the amended contract and
window metadata, not the pixel values; the 15-window repeat is the writer's independently logged mechanical check.
The bounded preparation goal is complete at the concrete researcher gate; the actual task and reference remain
unexecuted. Ch2 is advancing separately, so start-of-turn sync results are not an end-of-turn whole-workspace backup claim.

Exact limited decode recheck from this directory, using the recorded Python with rasterio:

```powershell
$windowCheck = @'
import json
from pathlib import Path
import rasterio
from rasterio.windows import Window
m=json.loads(Path('input_audit.json').read_text(encoding='utf-8'))
checks=0
for e in m['candidates']:
    with rasterio.open(e['recovery_path'],'r') as ds:
        for s in e['decode_spot_checks']:
            w=s['window']
            a=ds.read(w['band'],window=Window(w['col_off'],w['row_off'],w['width'],w['height']))
            assert sorted(set(int(v) for v in a.flat))==s['values'], e['id']
            checks+=1
print(f'PASS: {checks} exact-window decode checks reproduced; no full-raster or scientific validation')
'@
python -c $windowCheck
```
