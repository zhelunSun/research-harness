# 北京首轮产品描述案例准入

日期：2026-09-04。状态：研究者接受具体范围；不是 scientific gold 或方法效果接受。

## 直接授权

研究者对“采用这份 2025 多类图和既定北京城市森林边界，完成空间分布＋有效像元组成＋有依据的解释，暂不做真实面积估计、连通性和生态功能”回答“基本同意”，并要求继续推进、并行调研任务难度分级。

## 可执行范围

1. 接入 `beijing-task-family-v0.1/input_audit.json` 中的 strict500 多类图；只从恢复目录复制该文件到 canonical Chapter 3，同步记录源、目标 hash，不更改恢复目录。
2. 使用 canonical `BJuforebv4`；检查几何、CRS、覆盖与全部栅格可读性。保留类别 0–7 与 nodata 255；不重采样分析标签，不静默修补几何。
3. 在原网格按像元中心规则计算八类有效像元组成，形成地图、组成表及有限解释。比例不是面积占比；没有独立专题精度验证。参考数值是确定性产品描述，不是人工接受的 scientific gold。
4. 连通性与土地覆被分类制图作为第二波候选。区分复用冻结流程与重新训练，也区分结构复杂度与当前实施成本。并行调研仅形成候选分级，不冻结通用简单／复杂标准。

## 边界和停止点

不新做分类制图、GEE／Drive、模型比较、重训、连通性、真实面积、生态功能或用户研究。遇到无效几何、输入身份／类别冲突、没有有效覆盖时停止受影响计算并报告；普通工程验证和图表制作无需重复准入。

不改变 Idea、标题、章节角色、Ch2 优先级，也不替代 Route B 三案例的 matched-adapter 人工门。原候选材料保留历史正文，只增加本决定及实际成果指针。
